.DS_Store
node_modules/
tmp/
coverage/
*.log

# A deployment key must never be committed to a public repository.
.env
.env.*

# Built artefacts: the published page is index.html at the root; platform/ProjectX.html is
# the intermediate it is copied from, and public/ is assembled on every deploy.
platform/ProjectX.html
public/
package-lock.json
